# 취향 그리드 저장소 지침

## 성격

개인 연구용 도구. 서비스가 아니며 사용자는 저장소 소유자 한 명.
법적 검토, 이용자 동의 절차, 노출 통제는 이 저장소의 고려 대상이 아님.
외부 공개나 상업화로 전환할 경우 이 절을 먼저 갱신할 것.

## 트랙

### B. 취향 그리드

목적: 소비와 향유의 패턴을 좌표로 만들고 빈 영역을 찾음.

하위 갈래:
- 이론: 그리드 이론, 취향 벡터 이론(정렬 이론)
- 아이템 공간: 도메인별 장르와 유형을 한 공간에 배치
- 논문 코퍼스: 선행연구 수집과 흡수
- 사용자 좌표: 개인 소비 이력의 위치 산출
- 빈 영역 탐색: 사람은 있고 아이템이 없는 지점 추정
- 구현 수단: 임베딩, GraphRAG
- 인접 관심 주제: 파동함수 붕괴(좌표 확정), 탐정 기법(좌표 관측),
  렌더링(좌표 실현)

문서: `docs/taste-grid.md`, `docs/papers.md`

### C. 창작 출력

목적: 좌표를 읽는 데서 그치지 않고 그 자리에 만들어 넣음.

구성: 유튜브 다채널, 블로그, 음악, 글, 영상, 그림 생성,
스토리게임(Godot 4.x)

B와의 관계: C의 산출물이 B의 콘텐츠 좌표가 됨. 만들어봐야 좌표가 생김.

## 우선순위

미정. 갈래 간 우선순위는 지시받은 시점에 확정.
지시 없이 범위를 축소하거나 특정 갈래를 후순위로 처리하지 말 것.
제안은 자유. 다만 제안과 실행을 구분하고 실행은 승인 후에.

## 용어

- 축: 임베딩 공간의 차원. 우리가 산출하는 좌표를 가리킬 때만 이 표기를 씀
- 인용 용어: 논문에서 가져온 용어는 원문 표기 유지. Rentfrow의 five-factor model은 factor로 둠
- 병기: 혼동 가능한 자리에서는 원문 용어 뒤에 괄호로 우리 용어를 병기. 예) factor(우리 용어로는 축)
- 아이템 좌표: 콘텐츠 한 건의 임베딩 공간 내 위치
- 사용자 좌표: 사용자 한 명의 임베딩 공간 내 위치
- 정렬: 사용자 좌표와 아이템 좌표 사이의 거리 계산
- 클러스터: 사용자 좌표가 밀집한 영역
- 코퍼스: 논문 코퍼스와 아이템 코퍼스는 별개. 섞지 말 것

## 공통 규칙

### 문서

- 문체: 개조식 명사형 종결. 대시 사용 금지. 항목당 단일 명제
- 소급 적용 안 함: 기존 노트는 평서체 유지. 신규 문서부터 적용
- 미확정 명칭은 중립 표기 유지
- 다이어그램 산출물: 텍스트를 path로 변환하지 않은 SVG

### 데이터 취급

- 모든 수집 산출물에 수집 시점과 출처를 기록. 스냅샷 날짜 필수
- 외부에서 가져온 값과 이 저장소에서 산출한 값을 필드로 구분
- 원본 파일은 읽기 전용으로 열 것. 수정 금지
- 대용량 파일은 git에서 제외. 스냅샷 압축본만 커밋
- 임베딩 산출물에는 모델 이름과 버전을 메타데이터로 남길 것
- 민감 범주는 배제가 아니라 flag 필드로 표시. 사후 분리가 가능하도록
- 항목 유형은 type 필드로 구분. 장르와 브랜드는 같은 층이 아님

### 보고

- 수치와 표와 실패 목록을 우선. 해석은 요청받았을 때만
- 확보하지 못한 것은 사유와 함께 명시. 추정으로 채우지 말 것
- 관측된 사실과 판단을 문장 단위로 구분

### 대화

- 존댓말, 이모지 없음, 간결하게
- 단계별 안내는 4~5단계를 한 번에 제시. 단일 항목 확인으로 끊지 말 것
- 되돌리기 어려운 작업은 착수 전 경고
- 문제 해결 시 단순한 원인부터 확인. 로그와 상태를 먼저 볼 것

## 참조

- B 트랙 결정 근거와 폐기 대안: `docs/taste-grid.md`
- 논문 목록: `docs/papers.md`
- 논문 코퍼스: `corpus/`
- 아이템 코퍼스: `items/`. 나무위키 덤프 기반. `items/all.jsonl`, `items/genres.jsonl`, `items/proposals.md`, `items/failures/`
- 대화 원문을 근거로 인용하지 않음. 폐기된 안이 다수 포함되어 있음
